// This component is no longer used. Certificates now owns the training tab.
// Kept as an empty placeholder to avoid import errors if referenced elsewhere.
export default function Training() {
  return null;
}